// movies.js (Array mit Objekten)
// MovieList (List beinhaltet alle Items - alle Objekte aus Array)
// MovieItem (Item beinhaltet einen kompletten Datensatz)

// Header (beinhaltet Buttons mit Sortierfunktion) -> arbeiten mit array 

// useState setzen auf fertiges array 

// Feld um neue Filme hinzu zufügen 
    //  Form mit inputfeldern
    //  useState neu setzen

// Searchbar für Filme
    // Inputfeld
    // abgleich mit movies.title

// Filter nach Genre
    // Dropdown
    // erstelle array mit genre und dann abgleich mit movies.genre?

// Dynamische Unterseite für einzelne Filme
